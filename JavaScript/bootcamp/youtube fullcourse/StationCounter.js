//initialize the count at 0
let count = 0
//listen for clicks by having a function that the html looks at (Functions = function name(){codebody} )
function increase(){
    console.log("button was pushed to increase")

}
//when button is pushed we need to increment our current amount of people in the station
//After we make the change we need to make sure the HTML tag can reflect the new count that was added.